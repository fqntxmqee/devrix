# Proposal: devrix-d6-eval-cli

暴露 `devrix eval run` 子命令，将 EvalEngine 离线编排能力接入 CLI，默认 mock Judge 便于 CI/本地验收。

## Capabilities

| ID | 说明 |
|----|------|
| eval-cli-run | `devrix eval run` 加载 YAML 数据集并输出 JSON 报告 |

## L5

- L5-6-3-11 devrix eval run 子命令
