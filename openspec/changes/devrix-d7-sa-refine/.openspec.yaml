change_id: devrix-d7-sa-refine
priority: P0
demand_id: DM-20260614-008
status: s3_design
domains: [D7, ARCH]

# 切法 A：S 按「用户价值流」划分；旧 S2/S5 冻结为 Legacy Module Index
dsaft_scenarios:
  - D7-S2   # Session Orchestrator — 会话入口、FastPath、HandleInterrupt
  - D7-S3   # Wave Scheduler — DAG 调度、WorkerPool、ConflictGuard
  - D7-S4   # Execution Flow — FlowEvent 聚合、WorkPlan、IM 广播
  - D7-S5   # Decision & Planning — ClassifyIntent、SynthesizeTaskGraph

dsaft_activities:
  - D7-S2-A01  # ProcessMessage
  - D7-S2-A02  # EvaluateIntent
  - D7-S2-A03  # HandleInterrupt
  - D7-S3-A01  # ScheduleWave
  - D7-S3-A02  # ResolveWorkerContext
  - D7-S3-A03  # GuardConflict
  - D7-S4-A01  # PublishFlowEvent
  - D7-S4-A02  # SnapshotWorkPlan
  - D7-S4-A03  # NotifyGateway
  - D7-S5-A01  # ClassifyIntent
  - D7-S5-A02  # SynthesizeTaskGraph
  - D7-S5-A03  # SelectExecutor

t_points:
  - D7-S2-A01-T01       # ProcessMessage 入口验证
  - D7-S2-A03-T01       # HandleInterrupt 中断顺序
  - D7-S5-A01-T01       # ClassifyIntent 规则
  - D7-S5-A01-T02       # Command-first

version_scope:
  v1.0: Registry 重构（S2/S5 重编号 + Legacy 双轨）
  v1.1: S2 ProcessMessage 实现 + T 锚定
  v2.0: D7-S1 Task 模型归 D7（跨 D2 边界消除）

metrics_definitions: []

span_naming:
  - d7.session.orchestrate
  - d7.classify.intent
  - d7.wave.schedule
  - d7.flow.publish

legacy_frozen:
  module_index: D7-S1..S5（旧编号保留追溯）
  note: "旧 coordinator/wave/flow 模块索引冻结；新 Canonical 按价值流 S 编号"
