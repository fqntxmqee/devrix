change_id: devrix-reputation-feedback-loop
priority: P2
demand_id: DM-20260614-008
status: s1_proposal
domains: [D5, D6, D1, D2, D4, D7]

# 信誉/置信度/惩罚 — SoT 在 D5（可观测）+ D6（进化）；D1 仅 feedback 入站钩子
dsaft_scenarios:
  - D5-S2   # Metrics — signal quality / reputation gauge
  - D5-S1   # Tracer — user.feedback.* / d1.signal chain
  - D6-S3   # Eval — JudgeResult, ReputationStore, Delta
  - D6-S4   # Orchestration validation — runtime intervention

depends_on:
  - devrix-d1-sa-refine   # v1.1 IMOutboundSignal + d1.signal.* spans
  - devrix-d1-sa-refine    # S13 feedback 入站钩子

version_scope:
  v1.0: demand + proposal + registry delta（D5/D6 T/span）
  v1.1: ReputationStore + feedback span + Judge 闭环
  v2.0: EvolutionPolicy → D2/D4/D7 惩罚档位落地
